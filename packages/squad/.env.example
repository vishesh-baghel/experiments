# Squad Environment Variables

# Session Security (Required)
# Generate with: openssl rand -hex 16
SESSION_SECRET=your-32-character-secret-here

# Vercel Deploy Button (No OAuth required)
# The deploy flow uses Vercel's Deploy Button which handles repo cloning
# Callback URL: {APP_URL}/api/deploy/vercel/callback

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3001
SOURCE_REPO_NAME=experiments

# PostHog Analytics (Optional)
# Get your key at: https://posthog.com
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

